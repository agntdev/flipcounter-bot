# T02 — /start command + main menu

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 FLIP

Implement `/start`: a welcome message and the bot's main menu (inline keyboard) routing to its top-level features. Write the `/start` dialog spec to your OWN `tests/specs/T02.json` (a BotSpec array) and append the command to `tests/commands.json`.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui

Read `tasks/_skills/<name>.md` for each skill above before implementing.
