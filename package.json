{
  "name": "flipcounter-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Telegram bot that flips a coin on demand and tracks per-user flip counts",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@agntdev/bot-toolkit": "file:./packages/bot-toolkit",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
